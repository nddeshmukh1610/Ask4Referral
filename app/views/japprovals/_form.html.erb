<%= form_for(@japproval) do |f| %>
  <% if @japproval.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@japproval.errors.count, "error") %> prohibited this japproval from being saved:</h2>

      <ul>
      <% @japproval.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
